describe('initialize', () => {
  // test
  // .stdout()
  // .command(['initialize', '--config', 'local'])
  // .it('runs hello', ctx => {
  //   expect(ctx.stdout).to.contain('hello world')
  // })
})
